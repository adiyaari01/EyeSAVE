const axios = require("axios");
const baseURI = "http://localhost:8001"

// exports.getRequest = suffix =>axios.get(`${baseURI}/${suffix}`);
// exports.postRequest = (suffix, body) =>axios.put(`${baseURI}/${suffix}`,body);

